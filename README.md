# 机场导航站

**🇨🇳 中文** | [🇬🇧 English](README_EN.md)

一个采用玻璃拟态（Glassmorphism）设计风格的机场导航站，纯静态页面，支持动态数据管理，可直接部署到 Cloudflare Pages。

## 📸 预览

![项目预览](preview.png)

## ✨ 特性

- 🎨 **玻璃拟态设计** - 现代化毛玻璃效果，视觉精美
- 📱 **响应式布局** - 完美适配桌面端和移动端
- 🔍 **实时搜索** - 支持按名称/描述/域名搜索
- 🌓 **暗色模式** - 跟随系统或手动切换
- ⚡ **极速加载** - 纯静态页面 + CDN 加速
- 🛠️ **后台管理** - 可视化数据编辑，实时同步
- 📊 **SEO 工具** - 内置 SEO 分析诊断功能
- 🔗 **链接检测** - 一键检测失效链接
- 🚀 **一键部署** - 支持 Cloudflare Pages

## 📁 项目结构

```
导航站/
├── index.html        # 主页面
├── admin.html        # 后台管理面板
├── seo-tool.html     # SEO 分析工具
├── style.css         # 自定义样式
├── script.js         # 交互逻辑与数据
├── manifest.json     # PWA 配置
├── robots.txt        # 搜索引擎配置
├── sitemap.xml       # 站点地图
├── llms.txt          # AI 理解文件
├── functions/        # Cloudflare Pages Functions
│   └── api/          # API 接口
│       └── [[path]].js
├── wrangler.toml     # Cloudflare 配置
└── README.md         # 本文件
```

## 🛠️ 本地预览

**方式一：直接打开**
- 双击 `index.html` 文件即可在浏览器中查看

**方式二：使用本地服务器**
```bash
# 使用 Node.js 的 serve
npx -y serve ./

# 或使用 Python
python -m http.server 8080

# 或使用 VS Code Live Server 插件
```

## ☁️ 部署到 Cloudflare Pages

### 方式一：通过 GitHub 部署（推荐）

1. **创建 GitHub 仓库**
   - 登录 GitHub，点击 "New repository"
   - 填写仓库名称（如 `airport-nav`）
   
2. **上传代码**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/你的用户名/airport-nav.git
   git push -u origin main
   ```

3. **连接 Cloudflare Pages**
   - 登录 [Cloudflare Dashboard](https://dash.cloudflare.com)
   - 进入 **Workers & Pages** 
   - 点击 **Create application** → **Pages** → **Connect to Git**
   - 授权 GitHub 并选择仓库

4. **配置构建设置**
   - **Build command**: 留空
   - **Build output directory**: `/`

5. **环境变量配置（重要）**
   - 添加 `ADMIN_PASSWORD`: 你的管理密码
   - 绑定 KV 命名空间 `SITE_DATA` 用于存储数据

### 方式二：直接上传

1. 进入 Cloudflare Dashboard → **Workers & Pages**
2. 创建应用 → 选择 **Upload assets**
3. 拖拽项目文件夹上传

### 🌐 绑定自定义域名

1. 在 Pages 项目页面，点击 **Custom domains**
2. 添加你的域名（如 `nav.example.com`）
3. 按提示配置 DNS 记录

## 📝 自定义内容

### 修改导航数据

**方式一：使用后台管理（推荐）**
- 访问 `/admin.html`
- 登录后可视化编辑数据

**方式二：编辑代码**

编辑 `script.js` 中的 `siteData` 对象：

```javascript
const siteData = {
    tier1: [
        {
            name: "网站名称",
            desc: "网站描述",
            url: "https://example.com",
            domain: "example.com",
            icon: "🚀"
        },
    ],
    // 其他分类...
};
```

### 修改网站信息

- **标题**: 编辑 `index.html` 中的 `<title>` 和 `<h1>` 
- **配色**: 编辑 `body` 的渐变背景类名
- **分类**: 在 `script.js` 中的 `categories` 数组

## 🔧 技术栈

| 类别 | 技术 |
|---|---|
| 前端框架 | Tailwind CSS (CDN) |
| 后端 | Cloudflare Pages Functions |
| 数据存储 | Cloudflare KV |
| 部署 | Cloudflare Pages |

## 📊 SEO 优化清单

- [x] 响应式设计
- [x] 语义化 HTML
- [x] Meta 标签完善
- [x] Open Graph 社交分享
- [x] Schema.org 结构化数据
- [x] Sitemap.xml
- [x] Robots.txt
- [x] 暗色模式支持
- [x] 图标 alt 属性优化

## 📄 许可证

MIT License - 自由使用和修改
